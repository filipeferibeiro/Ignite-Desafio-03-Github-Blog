import styled from 'styled-components'

export const PublicationsList = styled.section`
  margin-top: 3rem;
  margin-bottom: 3rem;
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(26rem, 1fr));
  gap: 2rem;
`

export const HomeInfoContainer = styled.div`
  margin-top: 8rem;
`
